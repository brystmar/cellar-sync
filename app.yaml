# This file specifies your Python application's runtime configuration
# including URL routing, versions, static file uploads, etc. See
# https://developers.google.com/appengine/docs/python/config/appconfig for details.

# https://yamlchecker.com/

runtime: python38
#version: 1
#api_version: 1
#threadsafe: yes

includes:
  - env_variables.yaml

error_handlers:
  - file: templates/default_error.html

  - error_code: over_quota
    file: templates/over_quota.html

# Handlers define how to route requests to your application.
#handlers:

# App Engine serves and caches static files contained in the listed directories
# (and subdirectories). Uncomment and set the directory as needed.
#- url: /client
#  static_dir: client

# This handler tells app engine how to route requests to a WSGI application.
# The script value is in the format <path.to.module>.<wsgi_application>
# where <wsgi_application> is a WSGI application object.
#- url: .*  # This regex directs all routes to main.app
#  script: main.app
